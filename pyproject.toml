[project]
name = "rl-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.1.1",
    "matplotlib>=3.10.1",
    "tinygrad",
    "tqdm>=4.67.1",
    "typeguard>=4.4.2",
]

[tool.uv.sources]
tinygrad = { path = "tinygrad", editable = true }
